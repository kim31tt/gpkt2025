const revealItems=document.querySelectorAll(".reveal");revealItems.forEach((item,index)=>{setTimeout(()=>{item.classList.add("is-visible")},110*index)});const detail=document.getElementById("visitedDetail");const countryCards=document.querySelectorAll(".country-card");countryCards.forEach((card)=>{card.addEventListener("click",()=>{const flag=card.querySelector(".flag").textContent;const cities=card.dataset.cities;countryCards.forEach((item)=>item.classList.remove("is-selected"));card.classList.add("is-selected");detail.classList.add("is-changing");setTimeout(()=>{detail.innerHTML=`<div><span class="detail-flag">${flag}</span><p>${cities}</p></div>`;detail.classList.remove("is-changing");detail.scrollIntoView({behavior:"smooth",block:"nearest"})},180)})});const shell=document.querySelector(".js-tilt");if(shell&&window.matchMedia("(pointer: fine)").matches){shell.addEventListener("mousemove",(event)=>{const rect=shell.getBoundingClientRect();const x=event.clientX-rect.left;const y=event.clientY-rect.top;const rotateY=(x/rect.width-.5)*2.4;const rotateX=(y/rect.height-.5)*-2.4;shell.style.transform=`rotateX(${rotateX}deg) rotateY(${rotateY}deg)`});shell.addEventListener("mouseleave",()=>{shell.style.transform="rotateX(0deg) rotateY(0deg)"})}